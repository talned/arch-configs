# start a terminal
bindsym $mod+Return exec spawn $term
bindsym $mod+b exec spawn firefox
bindsym $mod+Shift+v exec spawn pavucontrol
bindsym $mod+n exec spawn spotify
bindsym $mod+CTRL+d exec spawn discord
# start dmenu (a program launcher)
bindsym $mod+slash exec --no-startup-id dmenu_run

